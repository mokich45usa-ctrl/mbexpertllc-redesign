export default function ServicesPage() {
  return (
    <main className="page-shell">
      <section className="hero">
        <div>
          <span className="eyebrow">Services</span>
          <h1>Service pages can now be structured cleanly from Sanity.</h1>
          <p className="section-copy">
            Each service can have its own title, short description, and full detail section.
          </p>
        </div>
      </section>
    </main>
  );
}
